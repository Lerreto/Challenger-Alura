FROM python:3-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

EXPOSE 8000

CMD ["python", "-u", "-c", "import time; print('Backend placeholder: API and RAG stack pending confirmation.', flush=True); time.sleep(10**9)"]
